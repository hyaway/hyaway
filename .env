# hyAway Environment Variables
# Local overrides go in .env.local (gitignored)
# Variables prefixed with VITE_ are exposed to the client.

# Base URL for the app (used in og:image, canonical URLs, etc.)
# Self-hosters should set this to their own domain
VITE_APP_URL=https://hyaway.com

# Debug: show swipe zone overlays in review feature
VITE_DEBUG_SWIPE_ZONES=false

# ─────────────────────────────────────────────────────────────────────────────
# ⚠️ DANGER ZONE: Preset Hydrus credentials
# ─────────────────────────────────────────────────────────────────────────────
# These values are embedded in the built JavaScript bundle and exposed to
# ALL users who access your hyAway instance. Only use for trusted private
# deployments where all users should have identical Hydrus access.
#
# VITE_HYDRUS_ENDPOINT=http://127.0.0.1:45869
# VITE_HYDRUS_ACCESS_KEY=your-64-character-access-key-here
